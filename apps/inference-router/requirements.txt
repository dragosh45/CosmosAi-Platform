# FastAPI provides the web framework for defining HTTP endpoints.
fastapi==0.136.3

# Requests lets this service call other internal HTTP services.
requests==2.34.2

# Uvicorn runs the FastAPI application as an ASGI server.
uvicorn==0.49.0
