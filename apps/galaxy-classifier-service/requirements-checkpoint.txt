# Use the CPU PyTorch wheel index so the optional demo does not require CUDA.
--extra-index-url https://download.pytorch.org/whl/cpu

# FastAPI provides the web framework for the /classify endpoint.
fastapi==0.136.3

# Uvicorn runs the FastAPI application inside the container.
uvicorn==0.49.0

# CPU-only PyTorch loads TinyGalaxyCNN checkpoints and runs inference.
torch==2.13.0+cpu
